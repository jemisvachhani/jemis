//Write a C program to calculate final electricity bill based upon below given criteria. take monthly electricity unit from user as input
#include<stdio.h>
void main()
{

    int unit,answer;
    printf("enter a unit");
    scanf("%d",&unit);
    answer=unit*2;
    
    if(answer=(unit*unit))
   {
      printf("enter a unit from monthly%d",unit);
   }
    
    
        
    
     


}

